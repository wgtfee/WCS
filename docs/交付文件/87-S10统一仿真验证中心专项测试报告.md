# S10 统一仿真与验证中心专项测试报告

## 1. 测试对象

S10 对以下软件范围执行专项验收：

- `SimulationVerificationOverviewController`；
- `SimulationVerificationApiService`；
- `SimulationVerificationViewModel`；
- `SimulationVerificationView`；
- Desktop 菜单、路由与 DI；
- Production / Simulation / HIL 环境边界；
- S0～S10 阶段能力矩阵；
- 真实 HIL、协议、机械安全、现场验收的非伪造约束。

## 2. 第一批 Functional Head

```text
Head: 1fab6709aa27c7c0c66519ec530626a977029014
Workflow: WCS S10 Unified Verification Console
Run: 30807550924 (#2)
Conclusion: success
```

第一轮 Run #1 暴露了 `SimulationGovernanceOptions.AllowedEnvironments` 在配置绑定时与默认数组叠加、造成重复值的问题。修复方式与 S9 HIL 配置一致：先把 `AllowedEnvironments` 初始化为空数组，再由 `ConfigurationBinder` 绑定。未删除测试、未降低门槛、未绕过 `Validate()`。

## 3. 专项结果

| 项目 | 结果 |
|---|---:|
| Simulator / Host Release 构建 | Success |
| Avalonia Desktop Release 构建 | Success |
| `SimulationUnifiedConsoleTests` | 10/10 Success |
| Production always 404 | Success |
| 非批准环境 fail-closed | Success |
| Simulation 环境能力矩阵 | Success |
| HIL 环境能力矩阵 | Success |
| S0～S10 顺序与数量 | Success |
| GET-only HTTP Surface | Success |
| Desktop 菜单、DI、路由 | Success |
| 页面安全文案 | Success |
| Real HIL / Mechanical / Site 默认 false | Success |

## 4. 10 条契约测试

1. Production 无条件返回 404；
2. Simulation 环境允许概览；
3. HIL 环境允许概览；
4. Development 等非批准环境返回 404；
5. Controller 只有一个 `HttpGet` Action；
6. 概览精确包含有序 S0～S10 共 11 阶段；
7. Simulation 环境开放 S0～S8，不开放 S9；
8. HIL 环境开放 S9，不声称 Simulation Runtime 可用；
9. 全局只读且不声称真实现场验收；
10. 全阶段只读，只有 S9 标记真实硬件要求。

## 5. 首批证据 Artifact

```text
Artifact ID: 8853417934
Name: wcs-s10-unified-verification-console-2
Expired: false
Head: 1fab6709aa27c7c0c66519ec530626a977029014
Digest: sha256:45985545b68289fbd6baa3fdee66f3a5adeef14d28a8c6045b7e3fe9f27b8c6d
```

此 Artifact 证明首批功能切片，不是最终 S10 Functional Head。后续文档和 Full Regression workflow 会移动 PR Head，因此最终验收必须在最终 Head 上重新执行专项门禁并取得新的 Artifact/Digest。

## 6. 最终软件门禁

S10 最终软件验收要求：

```text
WCS S10 Unified Verification Console = success
WCS S10 Software Full Regression = 46/46 exact-head success
One Hour Soak = success
PR Head unchanged during evidence collection
```

46-child 矩阵构成：

- S9 软件全回归已有 45 个 child；
- 新增 `WCS S10 Unified Verification Console` 1 个 child；
- 总数精确为 46；
- `realHilGateIncluded=false`；
- `realHilEvidenceRequiredForProjectCompletion=true`。

## 7. 安全结论

专项测试只允许得出：

> S10 统一仿真验证页面的软件能力通过验证，可用于受控环境下的只读检查。

不得得出：

- Real HIL 已执行；
- PLC/RGV 真实动作已验证；
- Protocol 已验收；
- Mechanical Safety 已验收；
- Site Acceptance 已完成；
- Production 已验收。

上述结论只能来自 S9 定义的真实 `[self-hosted,wcs-hil]` 现场证据链。
