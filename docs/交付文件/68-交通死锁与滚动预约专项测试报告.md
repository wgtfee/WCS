# 交通死锁与滚动预约专项测试报告

## 1. 报告状态

阶段：WCS Simulation & Verification v1.0 S4。

当前状态：S4 功能、专项测试和 35 项累计回归框架已建立。正式验收 Evidence 必须等最新 exact Head 首轮 35/35 完成后回填；本文在此之前不声明 S4 已完成。

## 2. 验证目标

- 冲突域定义和区段唯一归属；
- 单容量冲突与等待请求；
- 虚拟时间租约到期；
- 等待请求确定性再授权；
- 两车循环等待；
- 三车及多车强连通循环；
- Tarjan 等待图死锁检测；
- Priority、Sequence、VehicleId 受害者选择；
- 仿真内释放、取消与再授权；
- S3 路线上的滚动预约和已通过区段释放；
- StateStore、Checkpoint、Replay 和 State Hash；
- Host Production 404、未知运行 404 和只读检查；
- 有界容量和环形审计；
- 无生产控制链依赖。

## 3. 专项工作流

```text
WCS Simulation Virtual Traffic
WCS Simulation Traffic Deadlock Determinism
WCS Simulation S4 Full Regression
```

累计回归必须覆盖 35 条 exact-head 子工作流：历史 25 项、S0～S3 八条专项，以及 S4 两条专项。

## 4. 已建立测试

### 4.1 Virtual Traffic 综合测试

预期测试数量：12。

- 9 条核心交通/死锁测试；
- 3 条 Host Controller 边界测试；
- 构建、配置容量和静态隔离检查；
- CI 启用 `set -euo pipefail`，禁止 `dotnet test | tee` 掩盖失败；
- 工作流显式校验测试总数，防止过滤器漏测。

### 4.2 Deadlock Determinism 测试

预期测试数量：9。

覆盖：

- 冲突等待边；
- 租约到期；
- 两车死锁；
- 三车受害者选择；
- 滚动预约；
- 状态恢复；
- Replay 两次一致；
- Checkpoint 后继续与连续运行一致；
- 冲突域和等待请求容量。

### 4.3 阶段边界

专项工作流检查 S4 源码不得引用：

```text
IPlcClient
IPlcConnection
PlcWriter
CommandBus
UnifiedTransportDispatchEngine
TransportTrafficCoordinator
IRouteReservationManager
ReliableTransportVehicleDriver
TaskScheduler
TaskOrchestrator
TaskChainEngine
DeviceManager
```

并禁止 `Random.Shared`、`Task.Delay`、`Thread.Sleep` 和网络 Socket。

## 5. 首轮发现与修复

首轮开发测试发现并修复：

1. 两个含列表的 record 使用默认引用相等，导致状态恢复/Controller 快照测试误失败；改为比较规范化 JSON 内容；
2. 容量测试把等待请求误当作新预约；改为真实触发 `MaximumWaitingRequests`；
3. 工作流中的 `dotnet test | tee` 未启用 `pipefail`，可能掩盖测试失败；两条 S4 专项均已启用 `set -euo pipefail` 并校验预期测试数量；
4. 静态组合检查使用错误字面量；改为验证实际 `VirtualTrafficOptions` 组合类型。

上述修复没有删除测试、降低容量、使用 `continue-on-error` 或放宽安全门槛。

## 6. 正式验收条件

功能 Head 必须同时满足：

1. Virtual Traffic 专项 success，且 12/12；
2. Traffic Deadlock Determinism 专项 success，且 9/9；
3. S4 Full Regression success；
4. `evidence.json.workflowCount == 35`；
5. `allSuccess == true`；
6. 35 条 child 全部 `completed/success`；
7. 35 条 child `headSha` 全部等于 expected Head；
8. Full Regression 结束前 PR Head 未漂移。

首轮通过后回填 Run、Artifact 和 Digest，形成 Evidence Head。Evidence Head 必须再次完成相同 35/35，才允许 PR Ready 和 Squash 合并。

## 7. Evidence 回填区

```text
Functional Head:
Virtual Traffic Run / Artifact / Digest:
Traffic Deadlock Determinism Run / Artifact / Digest:
S4 Full Regression Run / Artifact / Digest:
workflowCount:
allSuccess:
Head verification:
```

## 8. 结论边界

当前只证明 S4 验证代码、专项门禁和累计回归框架已建立。尚未完成两轮 35/35，尚未合入 `develop`，也不代表真实交通控制、HIL、机械安全、制动距离或现场验收完成。
