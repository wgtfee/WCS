# 场景引擎确定性与回放测试报告

## 1. 验收范围

S1 测试覆盖严格 JSON DSL、虚拟时钟、Timeline 顺序、状态存储、断言、Checkpoint、恢复、Replay、Host API、运行注册表、取消语义和 S0 环境边界。

当前 PR：`#34 feat: add deterministic simulation scenario engine S1`。

## 2. 专项工作流

| 工作流 | 覆盖 |
|---|---|
| WCS Simulation Scenario Engine | DSL、Clock、Timeline、State、Checkpoint、Host API、RunRegistry、配置和控制隔离 |
| WCS Simulation Replay Determinism | 两次运行 Evidence/State/Assertion 等价、不同 SpeedFactor、Checkpoint 恢复 |
| WCS Simulation S1 Full Regression | 25 项 v3.9 历史 + S0 两项 + S1 两项，共 29 项 exact-head 回归 |

## 3. 核心断言

1. 未知 JSON 字段、注释、尾逗号和大小写错误被拒绝；
2. ScenarioId、Version、Seed 必须与 S0 Manifest 一致；
3. Timeline 按时间、类型、Order、Id 稳定排序；
4. 同时刻 Action 在 Assertion 前执行；
5. Pause 后不自动推进；
6. Step 仅执行一个 Timeline Item；
7. Advance 只向前推进；
8. SpeedFactor 不改变结果；
9. 相同 Seed 的随机序列一致；
10. Checkpoint 保存随机状态并可连续恢复；
11. Checkpoint 篡改、跨版本和错误 ManifestHash 被拒绝；
12. 10,000 Timeline Item 无丢失、无重复；
13. State、Timeline、Checkpoint 和 RunRegistry 容量有界；
14. 已登记内容 SHA 与执行内容不一致时拒绝；
15. Production、Development 和双开关缺失时 API 返回 404；
16. 取消后 Run 状态为 Cancelled，不能继续执行；
17. Replay 的 EvidenceHash、FinalStateHash 和 Assertions 全部一致；
18. S1 不包含 PLC 写、任务取消、路权释放、车辆选择和派单依赖。

## 4. Host API 测试链路

```text
S0 Validate/Register
→ S1 Create Run（Paused）
→ Step
→ Checkpoint
→ Resume
→ Run To Completion
→ FinalStateHash / EvidenceHash
→ Replay Twice
→ Equivalent=true
```

同时测试：未登记版本 404、错误 Base64 400、内容 SHA 不一致 400、未知 Run 404、终态重复操作 409。

## 5. 累计矩阵

S1 完整矩阵固定为 29 项：

- v3.9 历史 25 项；
- S0 Governance Compile；
- S0 Governance Boundary；
- S1 Scenario Engine；
- S1 Replay Determinism。

编排器必须：

- 读取 PR latest exact Head；
- 复用同 Head 已存在运行；
- 调度缺失工作流；
- 检查每个 child `head_sha`；
- 等待 29 项 `completed/success`；
- 结束前再次核对 PR Head 未移动；
- 生成 `runs.tsv`、`results.tsv`、`evidence.json`；
- Artifact 中 `workflowCount=29` 且 `allSuccess=true`。

旧的 `WCS Simulation S0 Full Regression` 是 S0 阶段专用编排器，不属于 S1 的 29 项累计矩阵。S1 以 `WCS Simulation S1 Full Regression` 为唯一累计编排门禁，并直接包含 S0 Compile 与 Boundary 两项。

## 6. 失败处理

- 编译或契约失败必须修实现；
- 不删除测试、不降低容量和安全门槛；
- 不使用 `continue-on-error`；
- Runner/SQL/网络波动必须读取 Job Logs 和 Artifact 后判断；
- 新提交产生新 Head 后，旧 Head 结果全部失效；
- 文档 Evidence 提交后必须再跑第二轮 29 项。

## 7. 首轮 exact-head 验收证据

首轮功能 Head：

```text
802b4c27d55a6062bae341fb83250bd78e09333c
```

| 工作流 | Run Number | Run ID | Artifact | Digest | 结果 |
|---|---:|---:|---|---|---|
| WCS Simulation Scenario Engine | #18 | `30432465347` | `wcs-simulation-scenario-engine-18` | `sha256:29c378ca1889c6aae38c12f7870e1a5ef9e2101d218cd93eb81c508020d38971` | success |
| WCS Simulation Replay Determinism | #18 | `30432465377` | `wcs-simulation-replay-determinism-18` | `sha256:7ce21ca14a09b8adb255521019639f8fd87c92bc462cfd55dd9ee7a8f564a767` | success |
| WCS Simulation S1 Full Regression | #4 | `30432465310` | `wcs-simulation-s1-full-regression-4` | `sha256:26ccfc0a50468036212fb6ea03548c6b423c0399dfd46500da8995065f8b3e4b` | success |

Full Regression Artifact 已核实：

```text
workflowCount = 29
allSuccess = true
all child status = completed
all child conclusion = success
all child headSha = 802b4c27d55a6062bae341fb83250bd78e09333c
PR latest Head = expected Head
```

29 项包括 Windows CI、One Hour Soak、End-to-End、Forecast、Adapter、PLC ML、Engine Load/Soak、Telemetry、Health、Governance、Root Cause、Maintenance、S0 Compile/Boundary 和 S1 两条专项。

## 8. 当前状态

- [x] latest exact Head Scenario Engine 成功；
- [x] latest exact Head Replay 成功；
- [x] 首轮 29/29；
- [x] Run、Artifact、Digest 回填；
- [ ] Evidence Head 第二轮 29/29；
- [ ] PR #34 Squash 合入 develop。

本次文档回填将生成新的 Evidence Head。首轮结果不能替代新 Head 的第二轮复验。

## 9. 现场边界

本报告只证明软件确定性、恢复和 API 边界，不证明 PLC 扫描、机械运动、车辆制动、现场网络和安全联锁。S2 以后逐步增加设备仿真，S9 单独进行 HIL/现场验收。
