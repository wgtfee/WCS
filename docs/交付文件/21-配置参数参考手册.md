# EMS/RGV、PLC 异常检测与统一仿真配置参数参考手册

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档版本 | V3.2 |
| 对应分支 | `feature/simulation-s8-capacity-hil-readiness` |
| 最新范围 | EMS/RGV 十二阶段、Telemetry、AnomalyEngine v1～v3.9、Simulation S0～S8 |
| 安全原则 | 诊断、维修建议、模型、Forecast 和仿真结果不得替代 PLC 联锁、设备安全回路、机械互锁或调度状态机；S8 HIL readiness 仅表示软件侧可进入 S9 准备，不表示真实 HIL/机械安全/现场验收已通过 |

本文汇总 `Wcs.Host` 主要配置。实际默认值以代码和 `appsettings*.json` 为准；生产修改必须经过版本、测试、审批和变更管理。

## 2. 配置源与凭据

配置优先级：

```text
appsettings.json
-> appsettings.{Environment}.json
-> 环境变量
-> 命令行或外部安全配置源
```

环境变量使用双下划线，例如：

```text
ConnectionStrings__WcsDb=...
AnomalyFusion__Enabled=true
AssetHealthGovernance__Enabled=true
AssetHealthRootCause__Graph__Version=site-graph-v1
AssetHealthMaintenance__RuleSet__Version=maintenance-rules-v1
AnomalyDetection__MachineLearning__PluggableRuntime__Enabled=true
AssetFailureForecast__Enabled=true
Simulator__Enabled=true
SimulationGovernance__Enabled=true
SimulationScenarioEngine__MaximumTimelineItems=100000
SimulationRunRegistry__MaximumRuns=1000
SimulationVirtualPlc__MaximumBlocks=128
SimulationVirtualRgv__MaximumVehicles=256
SimulationVirtualTraffic__MaximumZones=256
SimulationVirtualExternal__MaximumEndpoints=256
SimulationVirtualHealth__MaximumAssets=256
SimulationVirtualIntegration__MaximumMissions=256
SimulationCapacityReadiness__MaximumProfiles=16
SimulationCapacityReadiness__MaximumMissionsPerProfile=64
SimulationCapacityReadiness__MaximumConcurrentMissions=16
```

真实规则、Profile、Fusion Sources、PLC 点位、MES 密钥、根因图、维修规则、模型、数据集、许可证、场景、Manifest、真实 Endpoint 和审批信息不得以示例值写入通用生产配置。凭据必须由环境变量或安全配置中心注入。

## 3. SQL Server

| 参数 | 说明 | 生产建议 |
|---|---|---|
| `ConnectionStrings:WcsDb` | SQL Server 连接字符串 | 使用最小权限账号，禁止明文提交 |

SQL Server 承载任务、报警、异常生命周期、健康历史、事件、根因、维修反馈，以及 v3.9 模型版本、Forecast 和 Outcome Journal。v3.8/v3.9 模型文件与 active Manifest 存储在本地受控目录。

Simulation S0～S8 的场景、运行注册表、虚拟 PLC/RGV/Traffic/External/Health/Integration Mission、Capacity Profile/Sample/Audit、Forecast Oracle、Outcome 与 Evidence 都是仿真状态，不写生产 SQL，也不得作为生产任务、路权、外部系统、Forecast、容量许可或恢复状态源。

## 4. Simulator、SimulationGovernance 与真实 PLC

| 参数 | 说明 | 生产建议 |
|---|---|---|
| `Simulator:Enabled` | 模拟信号并禁止真实 PLC 后台服务 | Production 必须 `false` |
| `SimulationGovernance:Enabled` | 统一场景治理和 API 总开关 | Production 不得开启 |
| `ScenarioDirectory` | 场景文件目录 | 不存放真实生产数据 |
| `MaximumScenarioBytes` | 单场景内容上限 | 默认 1 MB，硬上限 16 MB |
| `MaximumRegisteredScenarioVersions` | 进程内 Registry 版本上限 | 默认 10,000 |
| `MaximumEvidenceRecords` | 单次 Evidence 记录上限 | 默认 10,000 |
| `MaximumEvidenceValueCharacters` | 单条 Evidence Value 字符上限 | 默认 4,096 |
| `AllowedEnvironments` | 允许访问 Simulation API 的环境 | 仅 `Simulation`、`SimulationLoadTest` |
| `WcsOptions:PlcPolling:Enabled` | 真实 PLC 轮询总开关 | 按现场验收开启 |
| `WcsOptions:PlcPolling:IntervalMs` | 基础轮询周期 | 50～1000ms，必须压测 |
| `PlcConnections` / `PlcBlocks` | S7 连接和 DB 块 | 使用正式点位版本 |

仿真治理必须同时满足：

```text
Simulator.Enabled=true
AND SimulationGovernance.Enabled=true
AND Environment in {Simulation, SimulationLoadTest}
AND Environment != Production
```

其他环境的 `/api/simulation/**` 返回 404。Production 即使错误配置 Simulator 或白名单，也必须 fail closed。

## 5. SimulationScenarioEngine S1

| 参数 | Simulation | SimulationLoadTest | 说明 |
|---|---:|---:|---|
| `MaximumTimelineItems` | 100,000 | 500,000 | 单场景 Action + Assertion 上限 |
| `MaximumStateEntries` | 10,000 | 50,000 | 单运行状态键上限 |
| `MaximumStateValueCharacters` | 4,096 | 4,096 | 单状态值和 Evidence Value 上限 |
| `MaximumCheckpointBytes` | 16,777,216 | 67,108,864 | Checkpoint canonical 内容上限 |
| `MaximumSpeedFactor` | 1,000 | 10,000 | 虚拟时钟倍率上限 |

S1 使用严格 JSON、确定性 Timeline、虚拟时钟、Checkpoint 和 Replay。速度倍率不得改变状态、断言或 Evidence 结果。

## 6. SimulationRunRegistry S1

| 参数 | Simulation | SimulationLoadTest | 说明 |
|---|---:|---:|---|
| `MaximumRuns` | 1,000 | 5,000 | 进程内运行数量上限 |

RunRegistry 仅为进程内仿真运行目录，不是生产任务恢复仓库。终态 Run 不再允许创建新的 Checkpoint inspection。

## 7. SimulationVirtualPlc S2

| 参数 | Simulation | SimulationLoadTest |
|---|---:|---:|
| `MaximumBlocks` | 128 | 256 |
| `MaximumBlockBytes` | 65,536 | 65,536 |
| `MaximumOperationBytes` | 65,536 | 65,536 |
| `MaximumScenarioTransferBytes` | 1,536 | 1,536 |
| `MaximumFaults` | 1,024 | 10,000 |
| `MaximumFaultPayloadBytes` | 1,536 | 1,536 |
| `MaximumAuditRecords` | 1,000 | 10,000 |

S2 使用进程内 DB 块和结构化故障；不得引用真实 PLC 客户端、Socket、PLC Writer、CommandBus 或调度组件。

## 8. SimulationVirtualRgv S3

| 参数 | Simulation | SimulationLoadTest |
|---|---:|---:|
| `MaximumVehicles` | 256 | 2,000 |
| `MaximumSegments` | 2,048 | 10,000 |
| `MaximumRouteSegments` | 256 | 1,000 |
| `MaximumAuditRecords` | 5,000 | 20,000 |
| `MaximumSegmentLengthMillimeters` | 10,000,000 | 100,000,000 |
| `MaximumSpeedMillimetersPerSecond` | 20,000 | 100,000 |
| `BatteryDrainBasisPointsPerMeter` | 1 | 1 |

S3 路线、位置、进度、电量和 Audit 进入 StateStore；运动只由虚拟时间 Action 驱动。

## 9. SimulationVirtualTraffic S4

| 参数 | Simulation | SimulationLoadTest |
|---|---:|---:|
| `MaximumZones` | 256 | 2,000 |
| `MaximumSegmentsPerZone` | 16 | 16 |
| `MaximumReservations` | 2,048 | 10,000 |
| `MaximumWaitingRequests` | 2,048 | 10,000 |
| `MaximumDeadlocks` | 512 | 5,000 |
| `MaximumAuditRecords` | 5,000 | 20,000 |
| `MaximumRollingLookAheadSegments` | 16 | 64 |
| `DefaultReservationLeaseMilliseconds` | 60,000 | 60,000 |
| `MaximumReservationLeaseMilliseconds` | 86,400,000 | 604,800,000 |

预约、等待、wait-for graph、Deadlock 和滚动预约只修改 Simulation State，不映射生产路权。

## 10. SimulationVirtualExternal S5

| 参数 | Simulation | SimulationLoadTest |
|---|---:|---:|
| `MaximumEndpoints` | 256 | 2,000 |
| `MaximumFaults` | 2,048 | 20,000 |
| `MaximumRequests` | 10,000 | 100,000 |
| `MaximumAuditRecords` | 5,000 | 50,000 |
| `MaximumRetryAttempts` | 16 | 32 |
| `DefaultTimeoutMilliseconds` | 5,000 | 5,000 |
| `MaximumDelayMilliseconds` | 86,400,000 | 604,800,000 |
| `CircuitFailureThreshold` | 3 | 5 |
| `CircuitOpenMilliseconds` | 30,000 | 30,000 |

支持 Mes、SqlServer、Http、Network、Custom 以及 Timeout、Unavailable、HttpStatus、InvalidResponse、DuplicateResponse、SqlDeadlock、SqlCommandTimeout、ConnectionReset、HighLatency、PacketLoss、HalfOpen。

S5 不保存真实 URL、ConnectionString、Token、密码或生产 Payload，不发真实 HTTP/SQL/Socket 请求。

## 11. SimulationVirtualHealth S6

| 参数 | Simulation | SimulationLoadTest |
|---|---:|---:|
| `MaximumAssets` | 256 | 2,000 |
| `MaximumSamplesPerAsset` | 2,048 | 10,000 |
| `MaximumForecastsPerAsset` | 512 | 2,000 |
| `MaximumOutcomesPerAsset` | 128 | 512 |
| `MaximumGeneratedSamplesPerAction` | 1,024 | 5,000 |
| `MaximumAuditRecords` | 10,000 | 50,000 |
| `MaximumRulHours` | 17,520 | 17,520 |

Forecast Oracle 必须满足：

```text
0 <= P24 <= P72 <= P168 <= 1
0 <= RulLower <= RulMedian <= RulUpper <= MaximumRulHours
```

Oracle 仅为 Simulation expected output，不加载真实模型、不调用生产 Forecast Service、不写生产 SQL、不触发自动控制。

## 12. SimulationVirtualIntegration S7

### 12.1 容量与策略参数

| 参数 | Simulation | SimulationLoadTest | 硬边界/说明 |
|---|---:|---:|---|
| `MaximumMissions` | 256 | 2,000 | 1～100,000；单运行集成 Mission 上限 |
| `MaximumSegmentsPerMission` | 16 | 64 | 1～1,000，同时不能超过 S4 rolling look-ahead |
| `MaximumAuditRecords` | 10,000 | 50,000 | 1～1,000,000；环形审计上限 |
| `ReservationLeaseMilliseconds` | 60,000 | 60,000 | 1ms～365 天，且不能超过 S4 最大租约 |
| `ExternalAckMaximumAttempts` | 3 | 5 | 1～100，且不能超过 S5 MaximumRetryAttempts |
| `ExternalAckTimeoutMilliseconds` | 5,000 | 10,000 | 必须在 S5 MaximumDelay 内 |
| `ExternalAckRetryDelayMilliseconds` | 1,000 | 1,000 | 0～365 天，且必须在 S5 MaximumDelay 内 |

### 12.2 集成链路

S7 只组合 S2～S6 Virtual Runtime：

```text
Virtual PLC
-> Virtual Traffic Reservation
-> Virtual RGV Movement
-> Virtual External Ack
-> Virtual Health Outcome
```

Mission 状态：

```text
Defined -> Dispatched -> Moving -> Completed -> Acknowledged
```

所有 Mission 与子系统状态进入同一 S1 `SimulationStateStore`，因此 Checkpoint、StateJson Restore、FinalStateHash 和 Replay 覆盖整条集成链。

### 12.3 exactly-once 与恢复

- External Ack 使用 MissionId 作为 IdempotencyKey；
- 重复 Ack 不创建第二条成功 Request；
- Health `CensoredNoFailure` Outcome 恰好一条；
- 恢复后不得残留该 Mission 的 Reservation、Waiting 或 Active Deadlock；
- Checkpoint resume 与不中断执行最终 StateHash、EvidenceHash、Assertions 必须一致。

### 12.4 安全边界

S7 Runtime 禁止真实 `HttpClient`、SQL/SqlSugar、Socket/TCP/UDP、真实 PLC/S7/Snap7、生产 CommandBus/TaskScheduler/TaskOrchestrator/DeviceManager、生产 Dispatch/Traffic/Reservation 组件、真实 Forecast Service/ONNX，以及墙钟等待和非确定性随机源。

## 13. SimulationCapacityReadiness S8

### 13.1 容量与资源参数

| 参数 | Simulation | SimulationLoadTest | 硬边界/说明 |
|---|---:|---:|---|
| `MaximumProfiles` | 16 | 64 | 1～10,000；进程内 Capacity Profile 上限 |
| `MaximumMissionsPerProfile` | 64 | 256 | 1～100,000；单 Profile Mission 上限 |
| `MaximumConcurrentMissions` | 16 | 64 | 1～MaximumMissionsPerProfile |
| `MaximumSegmentsPerMission` | 8 | 8 | 1～1,000，并受 S3/S4/S7 容量共同约束 |
| `MaximumSamplesPerProfile` | 512 | 2,000 | 1～1,000,000；有界 Sample 数量 |
| `MaximumAuditRecords` | 10,000 | 50,000 | 1～1,000,000；有界 Audit 数量 |
| `EightHourVirtualDurationMilliseconds` | 28,800,000 | 28,800,000 | 必须精确等于 8 个虚拟小时 |
| `TwentyFourHourVirtualDurationMilliseconds` | 86,400,000 | 86,400,000 | 必须精确等于 24 个虚拟小时 |
| `MaximumWallClockMilliseconds` | 120,000 | 180,000 | 单专项 Runner 软件预算；不是 8h/24h 墙钟运行 |
| `MaximumRssGrowthBytes` | 268,435,456 | 268,435,456 | 256 MB RSS 增长预算；失败不得通过放宽门槛解决 |

### 13.2 容量预检与守恒

S8 在创建任何 S2～S7 Virtual Resource 前，必须先执行组合容量 Admission Check。超出任一子系统容量时 fail-closed，不允许半初始化 PLC Block、Segment、Vehicle、Zone、Endpoint、Health Asset 或 Integration Mission。

S8 长稳 Evidence 至少覆盖：

```text
Profile state
Defined/Acknowledged mission conservation
State entry bound
Reservation/Waiting/Deadlock cleanup
External request exactly-once
Health outcome exactly-once
FinalStateHash / EvidenceHash
wall-clock elapsed / RSS growth / GC / thread / handle metrics
```

8h/24h 是加速虚拟时间；历史 `WCS One Hour Soak Load` 继续作为真实墙钟回归门禁。S8 不能把虚拟时间结果描述为真实设备连续运行结果。

### 13.3 software-side HIL readiness

`ReadyToEnterS9=true` 只表示仓库软件侧先决条件完成。以下外部验收在 S8 必须保持为 `false`：

```text
RealHilExecuted=false
MechanicalSafetyAccepted=false
SiteAccepted=false
```

真实 PLC/RGV、工业网络/协议、机械安全、现场点位/拓扑/凭据和试运行属于 S9/项目级验收。

## 14. 通用 WCS 运行时

- `WcsOptions:Persistence:IntervalSeconds`：通用状态持久化周期；
- `WcsOptions:Snapshot:IntervalSeconds`：快照周期；
- `WcsOptions:Snapshot:MaxSnapshots`：内存快照上限；
- `WcsOptions:AlarmMonitor` / `AlarmRules`：报警监控与防抖。

关键命令、路权、任务状态和安全状态不能只依赖周期快照。

## 15. PLC Telemetry

| 参数 | 说明 |
|---|---|
| `Storage:Telemetry:Provider` | `Disabled`、`SqlServer`、`InfluxDb` |
| `DurabilityMode` | `Buffered` 或 `WriteAhead` |
| `ChannelCapacity` | 有界队列容量 |
| `BatchSize` / `FlushIntervalMs` | 批量写入参数 |
| `RetryDelayMs` | Provider 失败重试间隔 |
| `WalBatchSize` / `WalFlushIntervalMs` | WAL 参数 |
| `SpoolDirectory` | 本地持久化目录 |
| `Site` / `Measurement` | 时序标签和 measurement |

必须监控 dropped、conservationDelta、queue、in-flight、WAL pending、目录容量和失败批次。

## 16. AnomalyDetection 与 ML

- v1：规则异常；
- v2～v3.3：ML、上下文、周期分析与 Fusion；
- v3.4：健康评分；
- v3.5：事件治理与 MES Outbox；
- v3.6：根因图；
- v3.7：维修决策与反馈；
- v3.8：可插拔本地模型/ONNX；
- v3.9：故障概率、RUL 与 Outcome。

真实模型、规则、图、数据集和许可证必须独立受控；仓库/Production 危险能力默认关闭。

## 17. Simulation Host API 安全

S0 场景登记、S1 运行控制、S2～S8 inspection 只允许隔离 Simulation 环境。

S7 只读 API：

```text
GET /api/simulation/virtual-integration/runs/{runId}/status
GET /api/simulation/virtual-integration/runs/{runId}/missions
GET /api/simulation/virtual-integration/runs/{runId}/missions/{missionId}
GET /api/simulation/virtual-integration/runs/{runId}/missions/{missionId}/consistency
GET /api/simulation/virtual-integration/runs/{runId}/audit?take=100
```

S8 只读 API 仅允许读取 capacity/HIL-readiness 状态与报告；不得提供直接执行长稳、故障注入、真实 HIL、PLC/RGV 控制或生产写入的绕过入口。

S2～S8 inspection API 均不得提供绕过 Scenario DSL/受控运行时的 PLC 写、车辆运动、路线修改、预约、死锁处置、Fault 注入、真实外部请求、生产 Forecast 写入、Mission Ack 或生产调度操作。

## 18. S8 首轮验证基线

Functional Head：

```text
44e8e877b1daa0a59d19285d3c8f95bafa0e6206
```

首轮：

```text
Capacity Long Stability #47 / Run 30765270280: 12/12 success
HIL Readiness Gate #46 / Run 30765270229: 5/5 success
S8 Full Regression #45 / Run 30765270288: workflowCount=43, allSuccess=true
43 条 child completed/success 且 headSha 全等于 Functional Head
WCS One Hour Soak Load Run 30765270222 success
PR Head verification expected == actual
```

首轮 Artifact：

```text
Capacity Long Stability:
ID 8838739363
sha256:f8320476667e90169917a019fc9c0eeb4857e4c57fb17ce2393e4fe329adf03f
Runner wall 3.17s / max RSS 110080 KB

HIL Readiness Gate:
ID 8838730285
sha256:1b86ffb7d1599553bf433dd59b8c98ef8a807af505924d886fbce63175e8492e
RealHilExecuted=false
MechanicalSafetyAccepted=false
SiteAccepted=false

S8 Full Regression:
ID 8839461345
sha256:af9457b13eb33c475e6e9a8f20d8fedf0aa4229b7739e9e94cc59e2bed096f1a
```

8h/24h 为 virtual-time evidence；文档 Evidence Head 必须再次通过 12/12、5/5、43/43，旧 Functional Head 成功不得替代新 Head 验证。

## 19. 变更、回退与生产检查

优先回退：

```text
SimulationGovernance__Enabled=false
-> Simulator__Enabled=false
-> 按需关闭 Forecast / PluggableRuntime / ML / Maintenance 等独立能力
```

生产检查：

- [ ] Environment 明确为 Production；
- [ ] `Simulator.Enabled=false`；
- [ ] `SimulationGovernance.Enabled` 未开启；
- [ ] AllowedEnvironments 不含 Production；
- [ ] `/api/simulation/**` 返回 404；
- [ ] S1～S8 Simulation 组件没有生产控制入口；
- [ ] S5/S7/S8 不含真实 Endpoint、ConnectionString、Token、密码或未脱敏 Payload；
- [ ] S6/S7/S8 不加载真实模型、不写生产 Forecast SQL；
- [ ] S7/S8 不引用真实 HTTP/SQL/Socket/PLC/RGV/生产调度组件；
- [ ] Registry、Evidence、Timeline、State、Run、Virtual PLC/RGV/Traffic/External/Health/Integration、Capacity Profile/Sample/Audit 和队列容量有界；
- [ ] `RealHilExecuted=false`、`MechanicalSafetyAccepted=false`、`SiteAccepted=false` 未被 S8 软件结果伪造为 true；
- [ ] 无 PLC 写入、设备停止、任务取消、生产路权释放、车辆选择、派单或调度修改；
- [ ] Simulation Evidence 不替代真实进程恢复、真实 HIL、机械安全、协议、MES/SQL/网络联调、真实模型验证和现场验收。