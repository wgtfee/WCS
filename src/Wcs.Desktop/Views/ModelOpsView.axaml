<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Wcs.Desktop.ViewModels"
             xmlns:atom="https://atomui.net"
             x:Class="Wcs.Desktop.Views.ModelOpsView"
             x:DataType="vm:ModelOpsViewModel"
             Background="{DynamicResource BgPrimary}">
    <Grid RowDefinitions="Auto,Auto,Auto,*,*" Margin="24" RowSpacing="14">
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto" ColumnSpacing="12">
            <StackPanel Spacing="4">
                <TextBlock Classes="wcs-page-title" Text="模型管理中心（ModelOps）" />
                <TextBlock Classes="wcs-page-subtitle" Text="AI 模型的版本注册、审批、影子运行、主备切换与隔离；所有操作只增审计、不可篡改。" />
                <TextBlock Classes="wcs-toolbar-status" Text="{Binding StatusText}" />
            </StackPanel>
            <atom:Button Grid.Column="1" Content="刷新" Command="{Binding RefreshCommand}" />
        </Grid>

        <Border Grid.Row="1" Classes="wcs-boundary-strip">
            <StackPanel Spacing="4"><TextBlock Classes="wcs-section-title" Text="安全边界"/><TextBlock Classes="wcs-page-subtitle" Text="{Binding SafetyText}"/></StackPanel>
        </Border>

        <atom:Card Grid.Row="2" Header="模型治理输入" StyleVariant="Outlined">
            <Grid ColumnDefinitions="*,*,*,*,*,Auto" ColumnSpacing="8">
                <StackPanel><TextBlock Text="模型ID"/><atom:LineEdit Text="{Binding ModelId}"/></StackPanel>
                <StackPanel Grid.Column="1"><TextBlock Text="资产类型"/><atom:LineEdit Text="{Binding AssetType}"/></StackPanel>
                <StackPanel Grid.Column="2"><TextBlock Text="配置档案"/><atom:LineEdit Text="{Binding Profile}"/></StackPanel>
                <StackPanel Grid.Column="3"><TextBlock Text="版本"/><atom:LineEdit Text="{Binding ModelVersion}"/></StackPanel>
                <StackPanel Grid.Column="4"><TextBlock Text="操作人"/><atom:LineEdit Text="{Binding Actor}"/></StackPanel>
                <atom:Button Grid.Column="5" VerticalAlignment="Bottom" Content="进入 Shadow" Command="{Binding EnterShadowCommand}" />
            </Grid>
        </atom:Card>

        <Grid Grid.Row="3" ColumnDefinitions="2*,*" ColumnSpacing="14">
            <atom:Card Header="部署状态" StyleVariant="Outlined">
                <Grid RowDefinitions="Auto,*" RowSpacing="8">
                    <Grid ColumnDefinitions="*,Auto,Auto,Auto" ColumnSpacing="8">
                        <TextBlock Classes="wcs-toolbar-status" Text="{Binding RecoveryText}" VerticalAlignment="Center"/>
                        <atom:Button Grid.Column="1" Content="批准 Champion" Command="{Binding PromoteChampionCommand}"/>
                        <atom:Button Grid.Column="2" Content="回滚 Fallback" Command="{Binding RollbackCommand}"/>
                        <atom:Button Grid.Column="3" Content="进入隔离区" Command="{Binding QuarantineCommand}"/>
                    </Grid>
                    <atom:DataGrid Grid.Row="1" ItemsSource="{Binding Deployments}" SelectedItem="{Binding SelectedDeployment}" AutoGenerateColumns="False" IsReadOnly="True" PaginationVisibility="None">
                        <atom:DataGrid.Columns>
                            <atom:DataGridTextColumn Header="版本" Binding="{Binding ModelVersion}" Width="120"/><atom:DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="110"/><atom:DataGridTextColumn Header="资产类型" Binding="{Binding AssetType}" Width="110"/><atom:DataGridTextColumn Header="配置档案" Binding="{Binding Profile}" Width="110"/><atom:DataGridTextColumn Header="操作人" Binding="{Binding Actor}" Width="140"/><atom:DataGridTextColumn Header="原因" Binding="{Binding Reason}" Width="*"/>
                        </atom:DataGrid.Columns>
                    </atom:DataGrid>
                </Grid>
            </atom:Card>
            <atom:Card Grid.Column="1" Header="操作理由" StyleVariant="Outlined">
                <StackPanel Spacing="8">
                    <atom:LineEdit Text="{Binding Reason}" AcceptsReturn="True" MinHeight="90" TextWrapping="Wrap"/>
                    <TextBlock Classes="wcs-section-title" Text="环境"/><TextBlock Text="{Binding Environment}"/>
                    <TextBlock Classes="wcs-section-title" Text="治理模式"/><TextBlock Text="{Binding ModeText}"/>
                    <TextBlock Classes="wcs-page-subtitle" Text="所有按钮仅改变 ModelOps 治理状态；不存在 PLC/CommandBus/调度写入。"/>
                </StackPanel>
            </atom:Card>
        </Grid>

        <atom:Card Grid.Row="4" Header="只增审计日志（不可篡改）" StyleVariant="Outlined">
            <atom:DataGrid ItemsSource="{Binding AuditEntries}" AutoGenerateColumns="False" IsReadOnly="True" PaginationVisibility="None">
                <atom:DataGrid.Columns>
                    <atom:DataGridTextColumn Header="时间" Binding="{Binding OccurredAtUtc}" Width="190"/><atom:DataGridTextColumn Header="操作" Binding="{Binding Action}" Width="180"/><atom:DataGridTextColumn Header="版本" Binding="{Binding ModelVersion}" Width="100"/><atom:DataGridTextColumn Header="操作人" Binding="{Binding Actor}" Width="120"/><atom:DataGridTextColumn Header="原因" Binding="{Binding Reason}" Width="*"/><atom:DataGridTextColumn Header="载荷 SHA-256" Binding="{Binding PayloadHash}" Width="260"/>
                </atom:DataGrid.Columns>
            </atom:DataGrid>
        </atom:Card>
    </Grid>
</UserControl>
