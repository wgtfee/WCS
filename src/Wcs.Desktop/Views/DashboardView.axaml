<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Wcs.Desktop.ViewModels"
             xmlns:atom="https://atomui.net"
             x:Class="Wcs.Desktop.Views.DashboardView"
             x:DataType="vm:DashboardViewModel"
             Background="{DynamicResource BgPrimary}">
    <ScrollViewer Padding="24">
        <StackPanel Spacing="16">
            <StackPanel Spacing="4">
                <TextBlock Classes="wcs-page-title" Text="运行总览" />
                <TextBlock Classes="wcs-page-subtitle" Text="设备、任务、报警、对象跟踪与资源锁的统一运行概览。" />
            </StackPanel>

            <UniformGrid Columns="5" Rows="1">
                <atom:Card Margin="0,0,6,0" StyleVariant="Outlined" IsHoverable="True"><atom:Statistic Header="设备总数" Value="{Binding DeviceCount}" ContentFontSize="26" /></atom:Card>
                <atom:Card Margin="6,0" StyleVariant="Outlined" IsHoverable="True"><atom:Statistic Header="进行中任务" Value="{Binding ActiveTaskCount}" ContentFontSize="26" /></atom:Card>
                <atom:Card Margin="6,0" StyleVariant="Outlined" IsHoverable="True"><atom:Statistic Header="活跃报警" Value="{Binding ActiveAlarmCount}" ContentFontSize="26" /></atom:Card>
                <atom:Card Margin="6,0" StyleVariant="Outlined" IsHoverable="True"><atom:Statistic Header="追踪对象" Value="{Binding TrackedObjectCount}" ContentFontSize="26" /></atom:Card>
                <atom:Card Margin="6,0,0,0" StyleVariant="Outlined" IsHoverable="True"><atom:Statistic Header="资源锁" Value="{Binding ActiveLockCount}" ContentFontSize="26" /></atom:Card>
            </UniformGrid>

            <atom:Card Header="快捷操作" StyleVariant="Outlined">
                <TextBlock Classes="wcs-page-subtitle" Text="运行入口由左侧导航统一承载，避免在总览页复制控制命令。" />
            </atom:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
